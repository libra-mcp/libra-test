---
rule-id: tailwind-patterns-components-states
enforcement: llm
---

# Rule: Components and Interaction States

## What to check
- Interactive elements include clear hover/focus states.
- Buttons/inputs maintain accessible touch targets and readable text sizes.
- Form errors include both visual and semantic indicators.
- Grid content with long text uses truncation-safe patterns (`min-w-0`, `truncate`).
