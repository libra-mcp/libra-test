---
rule-id: fixing-metadata-locale-alternates
enforcement: llm
---

# Rule: Locale and alternates (low-medium)

## What to check
Set the html lang attribute correctly. Set og:locale when localization exists. Add hreflang alternates only when pages truly exist. Localized pages must canonicalize correctly per locale.
